{
  "devDependencies": {
    "firebase-tools": "^15.21.0",
    "wait-on": "^9.0.10"
  },
  "scripts": {
    "start": "firebase --non-interactive emulators:start & wait-on --timeout 30s tcp:8181"
  }
}
